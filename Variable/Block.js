{
    // Statements inside the block
    let x = 10;
    console.log(x);
}

//Variables declared with let and const inside a block are block-scoped. This means they are only accessible within the block and any nested blocks.
//Variables declared with var are function-scoped or globally-scoped, so they are not limited to the block.

