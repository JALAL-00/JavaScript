//The join() method creates and returns a new string by concatenating all elements in an array, separated by a specified separator (default is a comma).

const fruits = ["Apple", "Banana", "Cherry"];
const result = fruits.join(" - ");

console.log(result); // Output: "Apple - Banana - Cherry"